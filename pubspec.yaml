name: ne_giyer
description: Shoppable TV - Dizi kıyafetlerini keşfet ve satın al.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.6

  intl: any
  url_launcher: ^6.3.2
  cached_network_image: ^3.4.1
  image_picker: ^1.0.7
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/data.json
